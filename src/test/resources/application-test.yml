spring.cloud.stream:
  kafka:
    binder:
      brokers: ${spring.embedded.kafka.brokers}
    streams.binder.brokers: ${spring.embedded.kafka.brokers}

spring.kafka.bootstrap-servers: ${spring.embedded.kafka.brokers}
